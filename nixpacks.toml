[phases.setup]
nixPkgs = ["python3", "nodejs"]

[phases.install]
cmds = [
    "echo 'Checking AUTH_PASSWORD environment variable...'",
    "if [ -z \"$AUTH_PASSWORD\" ]; then echo 'ERROR: AUTH_PASSWORD is not set!'; exit 1; fi",
    "node generate-config.js"
]

[start]
cmd = "sh start.sh"

